/** C3 — diagnosis prompt v1. Target: 10 hand-written wrong explanations, >=8 correct. */
export const DIAGNOSIS_PROMPT = '';
/** C5 — the level is set by OUR code and passed in as a constraint, never chosen by the model. */
export const COACHING_PROMPT = '';
